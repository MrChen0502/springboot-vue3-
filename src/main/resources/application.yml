server:
  port: 8010
  servlet:
    context-path: /api

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shop_db?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8
    username: root
    password: 123456
    hikari:
      data-source-properties:
        user: root  # ???? root ??????? Windows ?????

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 500MB

# MyBatis ??
mybatis:
  type-aliases-package: org.com.gx.ebusiness.entity
  mapper-locations: classpath:mapper/*.xml

logging:
  level:
    com.gx.ebusiness.mapper: debug